I hate explaining so im just gonna assume u've already got haxe and haxeflixel shit installed

Run `haxelib --global install hxpkg` and `haxelib --global run hxpkg setup`
Run `hxpkg install --force`
Run `haxelib run lime setup`